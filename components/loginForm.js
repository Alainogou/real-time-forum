function renderLoginForm(container) {
    // Rendu du formulaire de connexion
    container.innerHTML = `
        <h2>Connexion</h2>
        <!-- Ajoutez le formulaire de connexion ici -->
    `;
    // Ajoutez la logique de gestion du formulaire de connexion
}
