function renderCommentForm(container) {
    // Rendu du formulaire de commentaire
    container.innerHTML = `
        <h2>Ajouter un Commentaire</h2>
        <!-- Ajoutez le formulaire de commentaire ici -->
    `;
    // Ajoutez la logique de gestion du formulaire de commentaire
}
